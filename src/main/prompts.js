/**
 * prompts.js — src/main/prompts.js
 * Loads CLI tool prompt snippets used by the dynamic system prompt.
 * Layer: Main Process / Dependencies: none beyond static prompt data.
 */
export const CLI_PROMPTS = {
}
